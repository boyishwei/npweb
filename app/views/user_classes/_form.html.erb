<%= form_for(@user_class) do |f| %>
  <% if @user_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_class.errors.count, "error") %> prohibited this user_class from being saved:</h2>

      <ul>
      <% @user_class.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :classname %><br />
    <%= f.text_field :classname %>
  </div>
  <div class="field">
    <%= f.label :classdesc %><br />
    <%= f.text_field :classdesc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
